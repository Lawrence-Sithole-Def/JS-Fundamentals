const firstEmployee = {
  firstName: "David",
  lastName: "Tucker",
  department: "Engineering"
};

const secondEmployee = {
  firstName: "Sarah",
  lastName: "Smith",
  department: "Product"
};

console.log(`${firstEmployee.firstName} ${firstEmployee.lastName} - ${firstEmployee.department}`);
console.log(`${secondEmployee.firstName} ${secondEmployee.lastName} - ${secondEmployee.department}`);

const e = {
  fn: "Mina",
  ln: "Patel",
  d: "Engineering"
};

console.log(`${e.fn} ${e.ln} - ${e.d}`);
